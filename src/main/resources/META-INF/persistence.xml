<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="CortexPU" transaction-type="JTA">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <jta-data-source>java:jboss/datasources/CortexDS</jta-data-source>

        <!-- L2 Cache Mode: Cache only entities annotated with @Cacheable(true) -->
        <shared-cache-mode>ENABLE_SELECTIVE</shared-cache-mode>

        <properties>
            <!-- Dialect -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- Schema Management for Development -->
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>

            <!-- Formatting and SQL Logging -->
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Profiling and Statistics for Optimization / Sprint 4 -->
            <property name="hibernate.generate_statistics" value="true"/>

            <!-- HikariCP Connection Pool Settings -->
            <property name="hibernate.hikari.maximumPoolSize" value="16"/>
            <property name="hibernate.hikari.connectionTimeout" value="3000"/>
            <property name="hibernate.hikari.maxLifetime" value="1800000"/>
            <property name="hibernate.hikari.minimumIdle" value="4"/>
            <property name="hibernate.hikari.idleTimeout" value="600000"/>
            <property name="hibernate.hikari.poolName" value="CortexPool"/>

            <!-- JDBC Batching & Performance -->
            <property name="hibernate.jdbc.batch_size" value="25"/>
            <property name="hibernate.order_inserts" value="true"/>
            <property name="hibernate.order_updates" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
